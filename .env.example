# =============================================================================
# AI News Digest — Configuration
# =============================================================================
# This is THE file to edit for all app settings. Copy it to .env and fill in
# your values. Topics and keywords are configured per-user via the API after
# registration, not in this file.
# =============================================================================

# =============================================================================
# Application
# =============================================================================
APP_NAME="AI News Digest"
DEBUG=false
# Generate a strong random key for production (e.g. `openssl rand -hex 32`)
SECRET_KEY=your-secret-key-change-in-production

# =============================================================================
# Database
# =============================================================================
# SQLite (default, good for development — no setup needed)
DATABASE_URL=sqlite+aiosqlite:///./news_digest.db

# PostgreSQL (recommended for production)
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/news_digest

# =============================================================================
# AI Provider — controls which model summarizes articles
# =============================================================================
# Choose your AI provider: "anthropic" or "openai"
# You only need to set the API key for the provider you choose.
AI_PROVIDER=openai

# Anthropic (Claude) — used when AI_PROVIDER=anthropic
ANTHROPIC_API_KEY=your-anthropic-api-key
# Model to use for summarization (see https://docs.anthropic.com/en/docs/about-claude/models)
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# OpenAI (GPT) — used when AI_PROVIDER=openai
OPENAI_API_KEY=your-openai-api-key
OPENAI_MODEL=gpt-4o

# =============================================================================
# News API — source of articles
# =============================================================================
# Get your free API key at https://newsapi.org/
# Optional: if not set, only RSS feeds will be used as article sources.
NEWSAPI_KEY=your-newsapi-key

# =============================================================================
# Email (Resend) — delivers the digest
# =============================================================================
# Get your API key at https://resend.com/
RESEND_API_KEY=your-resend-api-key
# Must be an address on a domain you've verified with Resend
EMAIL_FROM_ADDRESS=onboarding@resend.dev
EMAIL_FROM_NAME="AI News Digest"

# =============================================================================
# Scheduling Defaults — applied to new users at registration
# =============================================================================
# Hour (0-23) and minute (0-59) when the daily digest is sent
DEFAULT_DIGEST_HOUR=8
DEFAULT_DIGEST_MINUTE=0
DEFAULT_TIMEZONE=UTC

# =============================================================================
# Limits
# =============================================================================
# Max topics a single user can create
MAX_TOPICS_PER_USER=5
# Max articles fetched per topic per digest
MAX_ARTICLES_PER_TOPIC=10
# Target max length (in tokens, approximate) for each article summary
SUMMARY_MAX_LENGTH=500
